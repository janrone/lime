import 'package:flutter/material.dart';

class Dtail extends StatelessWidget {
  const Dtail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('明细'));
  }
}
